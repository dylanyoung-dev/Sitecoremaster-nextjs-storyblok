import TopNavigationProps from './TopNavigation.interface';

const TopNavigation = ({ blok }: TopNavigationProps) => {
    return (
        <nav>
        
        
        </nav>
    )
}

export default TopNavigation;